#!/bin/bash

for folder in $(ls -d */ | egrep -v "(Urho3D|mycroft|\.Rcheck/)")
do

  cd $folder

  if [ -d ".git" ]
  then
    echo "git folder: "$folder
    git add --all :/
    git commit -m "Fix https://github.com/richelbilderbeek/mcbette/issues/18"
    git pull
    git push
  fi

  cd ..
done
