#!/bin/bash

for folder in $(ls -d */)
do

  cd $folder

  if [ -d ".git" ]
  then
    echo "git folder: "$folder
    git add --all :/
    git commit -m "Shortened 'create_copy_twin_tree_from_true_fun' to 'create_copy_twtr_from_true_fun'"
    git pull
    git push
  fi

  cd ..
done
